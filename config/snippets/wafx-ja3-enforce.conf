## WafX Advanced Bot Management — enforcement
##
## Include this inside `server {}` (or a specific `location {}`) to block
## connections whose JA3 is in the blocklist map.
if ($wafx_ja3_blocked) {
    return 403;
}

