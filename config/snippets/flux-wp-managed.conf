# Managed by Flux WAF dashboard — WordPress Security
# DEBUG CRS ONLY: semua directive tambahan dinonaktifkan sementara.
# Generated: 2026-03-25T02:21:16Z
# Include inside server { }:  include /etc/nginx/snippets/flux-wp-managed.conf;
#
# proxy_hide_header X-Powered-By;
#
# /wp-login.php only — zone key empty for other URIs (see http{} map $flux_wp_login_key)
# limit_req zone=flux_wp_login burst=5 nodelay;
#
# location = /xmlrpc.php {
#     deny all;
#     return 444;
# }
#
# location ~* /(wp-config\.php|\.env|\.htaccess|\.git) {
#     deny all;
#     return 404;
# }
#
# location ~* /wp-content/uploads/.*\.php$ {
#     deny all;
#     return 403;
# }
#
# if ($query_string ~* "author=[0-9]+") {
#     return 403;
# }
#
# if ($http_user_agent ~* "(WPScan|sqlmap|nikto|nmap|masscan|ZmEu|w3af|dirbuster|nuclei)") {
#     return 403;
# }
#
# location ~* \.(css|js)$ {
#     if ($arg_ver ~* "[0-9]+\.[0-9]+") {
#         rewrite ^(.*)$ $1? permanent;
#     }
# }
#
# Reminder — not enforced by nginx: set in wp-config.php:
#   define('DISALLOW_FILE_EDIT', true);

