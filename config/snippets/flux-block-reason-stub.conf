# DEBUG CRS ONLY — stub variabel untuk error page & header
#
# Ketika flux-403-block-reason.conf TIDAK di-include (mode debug tanpa GeoIP/JA3/JA4),
# $flux_block_reason_display tetap harus ada karena dipakai di location ^~ /errors/
# (sub_filter __FLUX_BLOCK_REASON__ / header X-Flux-Block-Reason pada build lama).
#
# SAAT MENGAKTIFKAN LAGI flux-403-block-reason.conf: hapus atau comment
# include flux-block-reason-stub.conf dari nginx.conf agar tidak bentrok (dua map ke variabel sama).

map $host $flux_block_reason_display {
    default "WAF (Coraza/CRS) — cocokkan Reference ID dengan coraza_audit.log.";
}
