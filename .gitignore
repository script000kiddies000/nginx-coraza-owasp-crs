# Cursor / IDE
.cursor/

# Local logs / runtime
logs/*

# Local baseline snapshot (pre-dashboard), not tracked
old code/

# Blueprints / research notes
blueprint*

templatewafx/

config/coraza/crs/.git/

# Local feature notes / benchmark results (not for public repo)
todo_fitur.md
benchmark-results.md

# MaxMind GeoLite2 database (binary, tidak perlu di-commit)
config/geoip/*.mmdb

# Runtime data directories (keep dirs via .gitkeep, ignore contents)
flux-data/*
!flux-data/.gitkeep
ssl_certs/*
!ssl_certs/.gitkeep


template/*
config/conf.d/
# Atau jika ingin spesifik:
config/conf.d/test.local.conf
config/conf.d/test2.local.conf
config/conf.d/xxx.com.conf
